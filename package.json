{
  "name": "xspire",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "front:build": "npm run build --prefix ./frontend",
    "back:build": "npm run build --prefix ./backend",
    "build": "npm-run-all front:build back:build",
    "start": "npm run start --prefix ./backend",
    "front:dev": "npm run dev --prefix ./frontend",
    "back:dev": "npm run dev --prefix ./backend",
    "preview": "start chrome http://localhost:3000",
    "dev": "npm-run-all --parallel front:dev back:dev preview"
  },
  "author": {
    "name": "Minh Do",
    "email": "mhdo@umass.edu"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/mhdo298/xSPIRE.git"
  },
  "private": true,
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
